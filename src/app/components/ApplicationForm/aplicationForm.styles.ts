import styled from 'styled-components';

const FormWrapper = styled.div`
  background: rgba(255, 154, 255, 0.9);
  border-radius: 10px;
  width: 450px;
  padding: 50px;
`;

export default FormWrapper;
