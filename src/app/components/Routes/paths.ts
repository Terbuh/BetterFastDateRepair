import services from './../__mock__/mock';

export const PATH_PAGE = {
  root: '/',
  login: '/login',
  register: '/register',
  contact: '/contact',
  aboutUs: '/aboutUs',
  details: `/services/${services.id}`
};
